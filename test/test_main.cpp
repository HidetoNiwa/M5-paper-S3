#include <unity.h>
#include "test_main.h"

// Test setup
void setUp(void) {
    // Set up each test
}

// Test teardown
void tearDown(void) {
    // Tear down each test
}

void app_main(void) {
    UNITY_BEGIN();
    RUN_TEST(test_example);
    UNITY_END();
}